import variable from '../variables/default';

export default (variables = variable) => {
  const bodyTheme = {
    flex: 1,
    alignItems: 'center',
    alignSelf: 'center',
  };

  return bodyTheme;
};
